defaults:
    - checkpoint: last 
    - path: standard
    - loss: standard
    - network: standard
    - trainer: standard
    - scheduler: cosine
    - optimizer: sgd
    - datamodule: cifar100

seed: 1234

# Currently we only want to loop over ratios.
# hydra:
#     sweep:
#         dir: ${path.logdir}
#         subdir: ratio_${prune.ratio}


